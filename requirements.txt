# Core agent framework
langchain>=1.3
langchain-openai>=0.3
langchain-community>=0.4
langchain-chroma>=1.1
chromadb>=0.5              # explicit: HttpClient used by app/tools/knowledge_base.py when CHROMA_SERVER_HOST is set (M6 Compose)
langchain-text-splitters>=1.1
langgraph>=1.2
langsmith>=0.4

# Web search tool (Tavily if TAVILY_API_KEY is set, else keyless DuckDuckGo fallback)
langchain-tavily>=0.2
ddgs>=9.0

# Memory backends
redis>=5.0
zep-cloud>=3.20

# Document parsing (knowledge base ingestion)
pypdf>=5.0
docx2txt>=0.9
pandas>=2.2

# Schemas / config
pydantic>=2.13
pydantic-settings>=2.14

# Interfaces
typer>=0.27
rich>=13.7
fastapi>=0.115
uvicorn[standard]>=0.32
streamlit>=1.40

# M5: LLMOps instrumentation, dashboards, security, evals
tiktoken>=0.8          # token counting for cost estimation (instrumentation.py)
plotly>=5.24           # monitoring dashboard charts
httpx>=0.27             # UI -> FastAPI HTTP client
pyjwt>=2.9               # optional JWT stretch (app/auth.py)

# Testing
pytest>=8.3
pytest-mock>=3.14
fakeredis>=2.26

# M6: load testing, Responsible AI bias audit (Categories 5-7)
locust>=2.31         # scripts/locustfile.py
aif360>=0.6.1         # scripts/bias_audit.py

python-dotenv>=1.0
